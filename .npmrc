# Suppress deprecation warnings from upstream/transitive packages
loglevel=error

# Suppress "N packages looking for funding" messages
fund=false

# Suppress vulnerability report on install (upstream devDep sub-dependencies).
# Run "npm audit" manually when you want to check.
audit=false
